import React from 'react';
import styles from './Effect.module.css';

const Effect = () => (
  <div className={styles.Effect} data-testid="Effect">
    Effect Component
  </div>
);

export default Effect;
