import React from 'react';
import styles from './Region.module.css';

const Region = () => (
  <div className={styles.Region} data-testid="Region">
    Region Component
  </div>
);

export default Region;
