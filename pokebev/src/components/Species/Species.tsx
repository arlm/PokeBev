import React from 'react';
import styles from './Species.module.css';

const Species = () => (
  <div className={styles.Species} data-testid="Species">
    Species Component
  </div>
);

export default Species;
